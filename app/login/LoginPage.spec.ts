import { test, expect } from "@playwright/test"
import LoginPage from "./page"
import { auth } from "@/auth"

  test("renders login inputs", async ({page}) => {
  })

  test("calls auth when logging in", async ({page}) => {
  })